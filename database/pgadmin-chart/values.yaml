

ldap:
  enabled: false
  hostname: ldap.rocket-sandbox.local.net
  name: ldapserver
  domain: aeo.rocketsoftware.com
  bind: "cn=admin,dc=aeo,dc=rocketsoftware,dc=com"
  user:
    name: admin
    password: admin
       
pgadmin:
  persistemVolumeClaim: pvc-pgadmin
  enabled: true
  hostname: pgadmin.local.net
  user:
    mail: admin@admin.com
    password: admin